nginx:
  replicaCount: 1

  service:
    type: ClusterIP

  ingress:
    enabled: true
    className: 'nginx'
    annotations:
      kubernetes.io/ingress.class: 'nginx'
    hostname: 'elliott.haus'
    tls:
      enabled: true
      secretName: 'elliott-haus-wildcard-tls'
