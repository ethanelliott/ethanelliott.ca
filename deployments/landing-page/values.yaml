replicaCount: 1

service:
  type: ClusterIP

ingress:
  enabled: true
  path: /
  annotations:
    kubernetes.io/ingress.class: 'nginx'
  hosts:
    - host: home.ee
      paths:
        - path: /
          pathType: Prefix
