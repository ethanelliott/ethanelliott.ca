apiVersion: v1
kind: Service
metadata:
  name: {{ include "splitwise.fullname" . }}
  labels:
    {{- include "splitwise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "splitwise.selectorLabels" . | nindent 4 }}
