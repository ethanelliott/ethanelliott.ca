{{- if .Values.ingress.enabled -}}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "splitwise.fullname" . }}
  labels:
    {{- include "splitwise.labels" . | nindent 4 }}
  annotations:
    link.argocd.argoproj.io/external-link: https://{{ .Values.ingress.hostname }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.ingress.hostname }}`)
      {{- if .Values.ingress.cors.enabled }}
      middlewares:
        - name: {{ include "splitwise.fullname" . }}-cors
      {{- end }}
      services:
        - name: {{ include "splitwise.fullname" . }}
          port: {{ .Values.service.port }}
  tls:
    certResolver: myresolver
{{- end }}
