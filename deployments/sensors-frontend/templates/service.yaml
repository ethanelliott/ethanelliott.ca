apiVersion: v1
kind: Service
metadata:
  name: {{ include "sensors-frontend.fullname" . }}
  labels:
    {{- include "sensors-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sensors-frontend.selectorLabels" . | nindent 4 }}
