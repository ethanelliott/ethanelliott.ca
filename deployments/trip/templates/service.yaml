apiVersion: v1
kind: Service
metadata:
  name: {{ include "trip.fullname" . }}
  labels:
    {{- include "trip.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trip.selectorLabels" . | nindent 4 }}
