apiVersion: v1
kind: Service
metadata:
  name: {{ include "split-frontend.fullname" . }}
  labels:
    {{- include "split-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "split-frontend.selectorLabels" . | nindent 4 }}
