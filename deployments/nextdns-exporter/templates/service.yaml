apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextdns-exporter.fullname" . }}
  labels:
    {{- include "nextdns-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "nextdns-exporter.selectorLabels" . | nindent 4 }}
