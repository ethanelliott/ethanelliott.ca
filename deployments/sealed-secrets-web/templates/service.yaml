apiVersion: v1
kind: Service
metadata:
  name: {{ include "sealed-secrets-web.fullname" . }}
  labels:
    {{- include "sealed-secrets-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sealed-secrets-web.selectorLabels" . | nindent 4 }}
