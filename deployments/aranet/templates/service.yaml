apiVersion: v1
kind: Service
metadata:
  name: {{ include "aranet.fullname" . }}
  labels:
    {{- include "aranet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aranet.selectorLabels" . | nindent 4 }}
