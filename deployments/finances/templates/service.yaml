apiVersion: v1
kind: Service
metadata:
  name: {{ include "finances.fullname" . }}
  labels:
    {{- include "finances.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finances.selectorLabels" . | nindent 4 }}
