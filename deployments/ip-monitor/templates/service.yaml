apiVersion: v1
kind: Service
metadata:
  name: {{ include "ip-monitor.fullname" . }}
  labels:
    {{- include "ip-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ip-monitor.selectorLabels" . | nindent 4 }}
