apiVersion: v1
kind: Service
metadata:
  name: { { include "recipes-frontend.fullname" . } }
  labels: { { - include "recipes-frontend.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "recipes-frontend.selectorLabels" . | nindent 4 } }
