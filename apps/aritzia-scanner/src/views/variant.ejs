<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= color %> - <%= product.name %></title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      .images {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 10px;
      }
      .image-container {
        width: 100%;
        height: 100%;
        overflow: hidden;
        border: 1px solid #ddd;
      }
      .image-container img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      .meta {
        color: #666;
        font-size: 0.9em;
        margin-bottom: 20px;
      }
      .lengths-list {
        margin-bottom: 20px;
      }
      .variant-length-wrapper {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 10px;
      }
      .variant-length {
        border: 1px solid #eee;
        padding: 10px;
        border-radius: 5px;
      }
      .variant-length.discontinued {
        opacity: 0.6;
        border-color: #d9534f;
      }
    </style>
  </head>
  <body>
    <%- include('partials/nav') %>

    <h1>
      <a
        href="/product/<%= product.id %>"
        style="color: inherit; text-decoration: none"
        ><%= product.name %></a
      >
      -
      <a
        href="/colors/<%= encodeURIComponent(color) %>"
        style="color: inherit; text-decoration: none"
        ><%= color %></a
      >
    </h1>

    <div class="meta">
      <p>
        <a
          href="https://www.aritzia.com/en/product/<%= product.slug %>?color=<%= color_id %>"
          target="_blank"
          style="color: #000; text-decoration: underline"
          >View on Aritzia</a
        >
      </p>
      <p class="meta">
        Added: <%= variants[0].added_at_formatted %> | Last Seen: <%=
        variants[0].last_seen_at_formatted %>
      </p>
    </div>

    <div class="lengths-list">
      <h3>Available Lengths</h3>
      <div class="variant-length-wrapper">
        <% variants.forEach(v => { %>
        <div class="variant-length <%= !v.isActive ? 'discontinued' : '' %>">
          <strong
            ><a
              href="https://www.aritzia.com/en/product/<%= product.slug %>?color=<%= v.color_id %>"
              target="_blank"
              style="
                color: inherit;
                text-decoration: none;
                border-bottom: 1px dotted #999;
              "
              ><%= v.length %></a
            ></strong
          >

          <% if (v.price) { %>
          <div style="margin-top: 5px">
            <% if (v.price < v.list_price) { %>
            <span style="color: #d9534f; font-weight: bold"
              >$<%= v.price %></span
            >
            <span
              style="
                text-decoration: line-through;
                color: #999;
                font-size: 0.9em;
              "
              >$<%= v.list_price %></span
            >
            <% } else { %>
            <span>$<%= v.price %></span>
            <% } %>
          </div>
          <% if (v.lowest_price && v.lowest_price < v.price) { %>
          <div style="font-size: 0.8em; color: #28a745">
            Lowest seen: $<%= v.lowest_price %>
          </div>
          <% } %> <% } %> <% if (v.all_sizes && v.all_sizes.length > 0) { %>
          <div style="margin-top: 10px; font-size: 0.8em">
            <% v.all_sizes.forEach(size => { %> <% const isAvailable =
            v.available_sizes.includes(size); %>
            <span
              style="display: inline-block; padding: 2px 5px; margin: 2px; border: 1px solid #ccc; border-radius: 3px; <%= !isAvailable ? 'text-decoration: line-through; color: #999; background: #f5f5f5;' : 'background: #fff;' %>"
            >
              <%= size %>
            </span>
            <% }); %>
          </div>
          <% } %>

          <br />
          <small
            >Added: <%= v.added_at_formatted %> | Last Seen: <%=
            v.last_seen_at_formatted %></small
          >
          <% if (!v.isActive) { %>
          <br />
          <small style="color: #d9534f; font-weight: bold">Discontinued</small>
          <% } %>
        </div>
        <% }); %>
      </div>
    </div>

    <h2>Images</h2>
    <div class="images">
      <% if (images && images.length > 0) { %> <% images.forEach(image => { %>
      <div class="image-container">
        <a href="/api/images/<%= image.id %>" target="_blank">
          <img
            src="/api/images/<%= image.id %>"
            alt="<%= color %>"
            loading="lazy"
          />
        </a>
      </div>
      <% }); %> <% } else { %>
      <p>No images available</p>
      <% } %>
    </div>
  </body>
</html>
