<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/" class="back-link">← Back to Home</a>
        <h1>✅ Success!</h1>
        <p class="subtitle">Bank account connected successfully</p>
      </header>

      <section class="success-section">
        <div class="success-card">
          <div class="success-icon">🎉</div>
          <% if (item) { %>
          <h2><%= item.institutionName || 'Bank Account' %> Connected</h2>
          <p>Item ID: <code><%= itemId %></code></p>
          <p>Connected at: <%= new Date(item.createdAt).toLocaleString() %></p>
          <% } else { %>
          <h2>Bank Account Connected</h2>
          <p>Your bank account has been successfully linked.</p>
          <% } %>
        </div>
      </section>

      <section class="next-steps">
        <h2>What would you like to do?</h2>
        <div class="action-buttons">
          <% if (itemId) { %>
          <a href="/accounts/<%= itemId %>" class="btn btn-primary"
            >View Accounts</a
          >
          <a href="/transactions/<%= itemId %>" class="btn btn-primary"
            >View Transactions</a
          >
          <% } %>
          <a href="/link" class="btn btn-secondary">Connect Another Account</a>
          <a href="/" class="btn btn-secondary">Back to Dashboard</a>
        </div>
      </section>
    </div>
  </body>
</html>
